.content
	= form_for [commentable, Comment.new] do |f|
		.form-group
			= f.text_area :post, class: "form-control", placeholder: "Add a comment"
		= f.submit "Comment", class: "button button-primary"
